export enum CoreMouseMode {
    SELECTION = 'selection',
    FRAME = 'frame',
    TEXT = 'text',
}

export enum ObjectType {
    GROUP = 'group',
    FRAME = 'frame',
    TEXT = 'text',
    ACTIVE_SELECTION = 'activeselection',
}